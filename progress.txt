# Progress Log

## Status: US-001 + US-006 Complete
## Current Story: Done (Track A)

---

### US-001: Playwright 설정 및 프로젝트 구조 (2026-04-01)

**Files changed:**
- `playwright.config.ts` — Created. Chromium only, baseURL localhost:3000, webServer pnpm dev
- `e2e/helpers/.gitkeep` — Created directory structure
- `e2e/fixtures/.gitkeep` — Created directory structure
- `package.json` — Added test:e2e, test:e2e:ui scripts; added @playwright/test devDep; updated playwright to 1.59.0
- `tsconfig.json` — Excluded playwright.config.ts and e2e/ from Next.js compilation
- `.gitignore` — Added /test-results/, /playwright-report/, /blob-report/

**Decisions:**
- Had to install @playwright/test (was missing, only playwright library existed)
- Updated playwright from 1.57.0 to 1.59.0 to match @playwright/test version
- Excluded playwright.config.ts and e2e/ from tsconfig to prevent Next.js build errors
- Used named export + eslint-disable comment for playwright config (requires default export)

**Verification:**
- `npx playwright test --list` lists 4 tests successfully
- `pnpm lint` passes (0 errors)
- `pnpm build` succeeds

---

### US-006: Editor E2E 테스트 (2026-04-01)

**Files changed:**
- `e2e/editor.spec.ts` — Created with 4 test cases
- `src/features/roadmap-editor/toolbar/components/ToolbarButton/index.tsx` — Added testId prop
- `src/features/roadmap-editor/toolbar/components/EditorToolbar/index.tsx` — Added data-testid to toolbar buttons
- `src/features/roadmap-editor/sidebar/components/EditorSidebar/index.tsx` — Added data-testid to empty state
- `src/features/roadmap-editor/properties/components/PanelHeader.tsx` — Added data-testid to panel header

**Test cases:**
1. Editor page loads and canvas renders
2. Node can be added via toolbar
3. Node selection shows properties panel
4. Ctrl+Z undo works

**Decisions:**
- Used `.react-flow` and `.react-flow__node` CSS class selectors for canvas/nodes (standard @xyflow/react classes)
- Added data-testid attributes to ToolbarButton, EditorToolbar, EditorSidebar, PanelHeader for reliable E2E selection
- Editor uses localStorage, no MSW needed
- Route: /editor/test-roadmap (dynamic [id] route with arbitrary ID)

**Verification:**
- `pnpm lint` passes (0 errors)
- `pnpm build` succeeds
